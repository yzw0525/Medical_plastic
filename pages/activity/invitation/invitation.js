// pages/activity/invitation/invitation.js
var appInstance = getApp();
Page({
  data: {
    imgpath: appInstance.globalData.urlpath + "/images/"
  },

  /**
   * 生命周期函数--监听页面加载
   */
  onLoad: function (options) {
  
  }
})